#include <stdio.h>

typedef struct 
{
    char product_name[26];
    int price;
} product;

void read_from_file(product *products, const int n)
{
    for (int i = 0; i < n; i++)
    { 
        fgets(pro[i].product_name, 25, is);
        for (int j = 0; j < 26; j++)
        {
            if (pro[i].product_name[j] == '\n')
            {
                pro[i].product_name[j] = '\0';
                break;
            }
        }
        fscanf(is, "%d\n", &pro[i].price);
    }
}

void print_to_screen(const product *products, const int n)
{
    for (int i = 0; i < n; i++)
        printf("%s\n%d\n", pro[i].product_name, pro[i].price);
}

int main()
{
    product pro[15];
    // const char file_mode = 'r';
    FILE *is = fopen("test.txt", "r");
    int n;
    fscanf(is, "%d\n", &n);
    if (n > 15)
    {
        puts("Error: ivalid size.");
        return -1;
    }
    if (feof(is))
    {
        puts("File Is empty without data.");
        return -2;
    }
    read_from_file(pro, n);
    int x;
    scanf("%d", &x);
    
    print_to_screen(pro, n);
    return 0;
}